name: Firmware verification

on:
  push:
  pull_request:

jobs:
  controller-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and run controller tests
        run: make test
        working-directory: projects/environment-controller
